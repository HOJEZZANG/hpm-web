# 소화기 점검 현황 연동

HPM의 `GET /api/fire-status?year=2026&month=09`는 원본 데이터베이스를 읽어 업체별 집계만 반환한다. 등록·점검·사진 입력은 기존 소화기 점검 시스템에서 수행한다. D1 migration, 데이터 복제, R2 변경은 없다.

## 확인한 원본

- 참조 저장소: `mindosa75/smart-factory`, 커밋 `7a6472a`.
- React 19 / Vite 8. 관리자 화면: `src/pages/AdminStatus.jsx`.
- 이력 읽기와 쓰기 필드 확인: `src/utils/storage.js`.
- 마스터 필드 확인: `src/pages/FireManagement.jsx`.
- 원본 저장소는 분석만 수행한다.

| 테이블 | 원본 코드에서 확인한 필드 | HPM이 SELECT하는 필드 |
| --- | --- | --- |
| `fire_extinguishers` | `id`, `no`, `company`, `factory`, `zone`, `manager` | `no,company` |
| `fire_inspection_history` | `id`, `no`, `company`, `factory`, `area`, `status`, `memo`, `photo`, `inspected_at` | `no,status,inspected_at` |

위 필드는 소스에서 확인한 것이며 전체 DB 스키마를 열거한 것은 아니다. HPM의 SELECT 필드는 실제 배포 조회에서도 유효함을 확인했다. 원본 관리자는 두 테이블을 `select('*')`로 읽지만 HPM은 위치, 사진, 메모, 담당자 필드를 요청하지 않는다. 원본 저장소에는 RLS 정책 SQL이 없으므로 정책 이름·정의는 소스만으로 확인할 수 없다. 배포된 Worker에서 publishable key로 두 테이블의 데이터 조회에 성공했다. HTTP 200의 빈 배열만으로는 데이터 부재와 RLS에 의한 행 필터링을 구별할 수 없으며, 이번 확인은 실제 마스터와 점검 이력이 모두 반환된 결과를 근거로 한다.

## 원본 집계 규칙

1. 현재 소화기 마스터를 `no` 순으로 읽고 마스터의 `company`로 묶는다. 과거 월을 선택해도 현재 마스터 기준이다.
2. 소화기와 점검 이력을 `no`로 연결한다. 이력의 업체명은 집계에 사용하지 않는다.
3. 해당 연월의 이력 중 `inspected_at`이 가장 최근인 하나를 사용한다. 같은 시각이면 응답에서 먼저 나온 기록을 유지한다. 원본 쿼리는 동률의 추가 정렬 기준을 정의하지 않았다.
4. 이력이 없으면 미점검이다. 이력이 있고 `status`가 정확히 `정상` 또는 `이상`이면 해당 건수를 더한다.
5. 업체별 점검률은 `Math.round(해당 월 이력이 있는 소화기 수 / 전체 * 100)`이다. 알 수 없는 상태값의 이력도 점검 완료 수에는 포함한다.
6. 전체 소계는 업체별 건수를 합하며 점검률은 `Math.round((정상 + 이상) / 전체 * 100)`이다. 따라서 누락·이전 상태값이 있는 경우 업체별 점검률과 소계의 계산 기준이 다르다. 원본과 동일하게 유지한다.
7. 전체가 0이면 점검률은 0이다.

원본은 브라우저 현지 시간으로 월을 판단한다. HPM은 국내 사용자 기준을 일관되게 유지하기 위해 한국 시간(Asia/Seoul)을 사용한다. 시간대가 없는 이전 형식은 한국 시간으로 처리한다. 원본의 잘못된 React 메모 의존성으로 생길 수 있는 일시적인 빈 화면은 재현하지 않는다.

누락 업체는 `미지정`으로 표시하고, 숫자형 이전 관리번호는 문자열로 정규화한다. 잘못된 날짜는 점검 이력에서 제외한다. 관리번호가 없는 마스터는 전체와 미점검에 포함한다. 업체명은 HTML로 해석하지 않는다.

## 조회·화면 동작

- 서버는 publishable 형식만 허용하고 `apikey` 헤더만 사용한다. JWT로 취급하거나 권한 상승 키를 사용하지 않는다.
- 해당 월 시작 이상·다음 월 시작 미만 조건으로 이력을 SELECT한다. 페이지별 실제 건수와 전체 건수를 확인해 페이지 제한에 따른 누락을 방지한다.
- 요청 전체에 12초 제한을 둔다. 각 테이블 50,000건 초과 또는 불완전한 응답은 일부 합계를 표시하는 대신 오류로 처리한다.
- 캐시는 `no-store`, CORS Origin은 기존 `https://hojezzang.github.io`를 유지한다.
- 오류 로그에는 종류, 테이블명, HTTP 상태만 기록한다. 외부 응답 본문·키·헤더·URL은 기록하지 않는다.
- 외부 요청은 `redirect: 'manual'`로 전송하고 3xx 응답을 거부한다. Workers 런타임과 호환되며 다른 주소로 키를 전달하지 않는다.
- 연월 변경과 화면 재진입 시 최신 데이터를 조회한다. 조회 중 이전 표를 지우며 같은 연월의 진행 중 요청은 중복 실행하지 않는다. 연월 변경 시 이전 요청을 취소하고 오래된 응답을 무시한다.
- 모바일에서는 현황표 영역을 좌우로 스크롤한다. 기존 사이트 열기는 새 창을 사용한다.
- 등록·수정·삭제 API나 화면은 추가하지 않는다. 기존 로그인·일정·문제점·사진 흐름은 그대로 유지한다.

## 검증

`npm.cmd run check`, `npm.cmd test`, `node scripts/browser-check.mjs "<Chrome 실행 파일 경로>"`를 실행한다. 브라우저 검사는 운영 API 요청을 모두 차단하고 mock으로 기존 CRUD·사진·일정·발표 기능 및 새 현황 화면을 검증한다.

실제 운영 회귀 검사는 `node scripts/check-remote-calendars.mjs`를 옵션 없이 실행한다. `--write` 옵션은 이번 작업에서 사용하지 않는다.

배포 후 웹사이트에서 소화기 메뉴 → 연월 변경 → 새로고침 → 다른 메뉴 후 재진입 → 모바일 표 스크롤 → 기존 소화기 시스템 열기를 확인한다. 기존 문제점·사진·일정의 운영 확인은 조회만 수행한다.

2026-09-24 검증 결과: 자동 테스트 41개, 문법·설정 검사, Chrome 브라우저 회귀 검사 통과. 운영 API의 2026년 9월 응답은 9개 업체, 전체 50, 정상 4, 이상 0, 미점검 46, 점검률 8%였다. 운영 health·문제점·AS·팀 일정 조회와 CORS 검증도 통과했고 기존 세 컬렉션의 데이터 해시는 배포 전후 동일했다. 운영 문제점에 연결된 사진은 없어 실제 R2 사진 GET은 검증할 대상이 없었으며 사진 업로드·조회·삭제·미리보기·확대·조치 전후 사진은 mock 및 로컬 DB 테스트로 검증했다. 운영 데이터에는 쓰기를 수행하지 않았다.
